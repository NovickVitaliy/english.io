{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "MongoOptions": {
        "ConnectionString": "mongodb://localhost:27017",
        "DatabaseName": "learning-db"
    },
    "GeminiOptions": {
        "GenerateContentUrl": "https://generativelanguage.googleapis.com/"
    },
    "AiLearningPrompts": {
        "PromptForWordTranslatingWithExampleSentences": "You are a language-learning assistant for English learners whose native language is Ukrainian; analyze a single English word and return ONLY structured JSON in this schema: {'word':'string','partOfSpeech':'string','senses':[{'senseId':1,'definition':'string','ukrainianTranslation':'string','usageLabel':'common|formal|informal','examples':['string',... up to {EXAMPLES_COUNT} strings],'synonyms':['string',... up to 3]}]}, with max 3 senses, only common learner-relevant meanings, each sense must have a sense-specific Ukrainian translation, examples must be simple B1–B2, exactly {EXAMPLES_COUNT} examples per sense, no idioms or phrasal verbs unless the word itself is one, ensure senses and synonyms are clearly distinguishable and non-overlapping, output JSON only, no explanations, markdown, or prose; INPUT: Word: {WORD}",
        "PromptForCheckingIfWordCompliesToTheTopic": "Does the word '{word}' relate to the topic '{topic}'? Respond with a JSON object: {'doesComply': true or false} Ensure the response is valid JSON without any extra text.",
        "PromptForCheckingIfTranslationsAreCorrect": "You are an English–Ukrainian dictionary verification assistant; analyze the array of user-provided translations in 'TranslatedWords' and return ONLY structured JSON in this schema: [{'OriginalWord':'string','TranslatedWord':string|null,'IsCorrect':boolean,'SenseId':'string'}]. You MUST return exactly one output object for EACH input item in TranslatedWords, in the SAME order. OriginalWord MUST be copied from input. TranslatedWord MUST be copied exactly from the user's 'Translated' field (keep it null if the user provided nothing). SenseId MUST be copied exactly from the input item without modification. IsCorrect indicates whether the translation is correct for the meaning described in 'Definition' when translating from OriginalLanguage to TranslatedLanguage using standard English–Ukrainian dictionary rules. If Translated is null, empty, or incorrect, IsCorrect MUST be false. Do NOT include correct translations. Do NOT omit any items. Do NOT explain anything. Output JSON ONLY. INPUT: OriginalLanguage: {OriginalLanguage}, TranslatedLanguage: {TranslatedLanguage}, TranslatedWords: {TranslatedWordsJson}",
        "PromptForGeneratingSentencesWithGaps": "You are a sentence-generation assistant for a language-learning app; INPUT: WordForPracticeJson (JSON array): {WordForPracticeJson} with items containing PartOfSpeech, WordId, Word, SenseId, Sense; TASK: for each input item generate exactly one natural B1–B2 English sentence matching the Sense, replace the Word with exactly five asterisks (*****), ensure the context allows guessing the missing word and only that word is missing; CRITICAL: senseId MUST be copied character-for-character from the input SenseId, treat it as immutable, do NOT modify, reformat, regenerate, normalize, or invent it; OUTPUT: return ONLY a valid JSON array where each object has exactly {'sentence':'string','correctWord':'string','senseId':'guid'}, mapping rules: one input → one output, correctWord = input Word, senseId = input SenseId verbatim, no explanations, no extra fields, JSON only.",
        "PromptForGeneratingExampleText": "Generate a short, engaging story, dialogue, or article that naturally incorporates the following words: [{words}]. The text should be coherent, meaningful, and demonstrate the correct usage of each word in context. Aim for a balance between variety and clarity so the user can understand how these words are used in real-life situations. Return a json object of next structure: {text: string}. Every word that I gave you put inside *.",
        "PromptForGeneratingReadingComprehensionExercise": "Create a short, coherent English text of 100–150 words that naturally uses each word exactly once from the provided input array in the meaning described by its Sense; INPUT is a JSON array of objects WordForPractice with fields {PartOfSpeech, WordId, Word, SenseId, Sense}; Here is json: {WordsForPracticeJson} ensure every Word is used according to its Sense (do not change form in a way that breaks the sense); after the text, generate exactly 3 reading comprehension questions that test understanding of the text (not vocabulary definitions); OUTPUT ONLY JSON in this exact format: { 'Text': string, 'Questions': string[] }, with no explanations, no markdown, and no extra fields.",
        "PromptForCheckingIfReadingComprehensionExercise": "Given the following text, questions, and user answers, check each answer based on the text. For each, return whether it is correct and what the correct answer is. Then return JSON: { \"AnswersCorrect\": int, \"AnswersResults\": [ { \"IsCorrect\": bool, \"CorrectAnswer\": string } ] }. Text: {text} Questions: {questions} Answers: {answers}",
        "PromptForCheckingSentencesWithGapsTask": "You are a sentence-gap verification assistant; INPUT (JSON): {SentenceWithFilledGapJson}; TASK: for each item in the input array (fields: Sentence, CorrectWord, FilledInWord, SenseId) determine IsCorrect using soft matching—FilledInWord must not be null or empty and must match CorrectWord ignoring case while allowing only minor typos (1–2 characters: missing/extra letter or simple keyboard slip), but reject synonyms, translations, different roots, different tenses, plurals, or derivations; OUTPUT ONLY a JSON array in the same order with EXACT fields [{Sentence:string, CorrectWord:string, SenseId:string, IsCorrect:boolean}], where Sentence, CorrectWord, and SenseId are copied from input and IsCorrect is computed, with no explanations or extra text."
    },
    "ConnectionStrings": {
        "RedisCache": "localhost:6379"
    },
    "NotificationsApi": {
        "Http": "http://localhost:5200/",
        "Https": "https://localhost:5201/",
        "IsHttps": false
    }
}
